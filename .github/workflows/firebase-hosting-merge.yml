# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: install dependencies
        id: step_one
        run: npm install --production
      - name: run build
        env:
            FIREBASE_CONFIG: ${{ toJson(secrets.FIREBASE_CONFIG) }}
            FIREBASE_CONFIG2: ${{ secrets.FIREBASE_CONFIG }}
            TESTING: ${{ secrets.TESTING }}
            BLA: 'Blupp'
        id: step_two
        run: |
            #echo $FIREBASE_CONFIG > firebase-config.json
            #echo $TESTING > testing.txt
            echo $BLA
            #cat testing.txt
            npm run build --production --bla=$BLA --FIREBASE_CONFIG=$FIREBASE_CONFIG2 --FIREBASE_CONFIG2=$FIREBASE_CONFIG
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_TODO_ANDROID_APP_870EE }}'
          channelId: live
          projectId: todo-android-app-870ee
          FIREBASE_CONFIG: '${{ secrets.FIREBASE_CONFIG }}'
